# Git Workflow Guide for Triadic

## Quick Push Script

Use the auto-push script for quick updates:

```bash
# Push with default message
./push_to_github.sh

# Push with custom message
./push_to_github.sh "Fixed web search functionality"
```

## Manual Workflow

### Making Changes and Pushing Updates

1. **Make your code changes** (edit files, etc.)

2. **Check what changed:**
   ```bash
   git status
   ```

3. **Stage your changes:**
   ```bash
   # Stage all changes
   git add .
   
   # Or stage specific files
   git add app.py utils/streamlit_ui.py
   ```

4. **Commit your changes:**
   ```bash
   git commit -m "Description of what you changed"
   ```

5. **Push to GitHub:**
   ```bash
   git push origin main
   ```

### Useful Commands

**View changes before committing:**
```bash
git diff
```

**View commit history:**
```bash
git log --oneline
```

**Undo changes (before committing):**
```bash
# Discard changes to a file
git checkout -- filename.py

# Discard all uncommitted changes
git reset --hard HEAD
```

**Update from remote (if working on multiple machines):**
```bash
git pull origin main
```

## Streamlit Cloud Deployment

Once you push to GitHub, Streamlit Cloud will automatically:
- Detect the push
- Redeploy your app with the latest changes

You can also manually trigger a redeploy from the Streamlit Cloud dashboard.
